<%
    let showText = "Pokaż skrypt dla tego zadania";
    let hideText = "Ukryj skrypt dla tego zadania";
    let showScript = (scriptContent.match(/\n/g) || "").length + 1 < 50;
%>
<div class="mmj-script">
    <button id="scriptButton" class="btn btn-dark" data-show="<%= showText %>"
        data-hide="<%= hideText %>"><%= showScript ? hideText : showText %></button>

    <pre id="scriptPreview"
        class="<% if(!showScript) { %> mmj-hidden<% } %>"><%- highlight("javascript", scriptContent).value %></pre>
</div>
